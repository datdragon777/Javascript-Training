{
  "name": "big-practice",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "server": "json-server --watch json-server/db.json",
    "start": "parcel src/index.html",
    "build": "rm -rf build && rm -rf .parcel-cache && parcel build ./src/*.html --no-cache --no-source-maps --dist-dir dist"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "eslint": "^8.44.0",
    "parcel": "^2.9.3"
  }
}
